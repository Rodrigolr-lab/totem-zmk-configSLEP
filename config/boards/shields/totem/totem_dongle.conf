# TOTEM dongle (central over USB). Split role / peripheral count / BT limits are
# set in Kconfig.defconfig. This file is for user tuning of the dongle build.

# The dongle is USB-powered and should stay awake so the halves stay connected.
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=1800000

# ZMK Studio: live keymap editing in the browser over USB (https://studio.zmk.dev).
# The studio-rpc-usb-uart snippet (build.yaml) adds the USB transport; this turns
# Studio on. Unlock for editing with &studio_unlock (bound on the ADJ layer).
CONFIG_ZMK_STUDIO=y
